---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PageCTA from '../components/PageCTA.astro';

const articles = [
  {
    date: 'March 2026',
    category: 'Sustainability',
    title: 'The Road to Net-Zero: Modular Sustainability',
    excerpt:
      'How factory-controlled builds, material optimization, and logistics discipline stack up to measurable carbon reductions across the project lifecycle.',
    href: '#',
  },
  {
    date: 'Feb 2026',
    category: 'Government',
    title: 'DoD Procurement: What You Need to Know',
    excerpt:
      'A practical overview of requirements, small-business pathways, and documentation that primes and subs expect on defense-adjacent modular programs.',
    href: '#',
  },
  {
    date: 'Jan 2026',
    category: 'Partnership',
    title: 'HWB Partnership Expands Healthcare Reach',
    excerpt:
      'Modular clinics and humanitarian infrastructure gain momentum as GMP and Hospitals Without Borders align delivery models for underserved regions.',
    href: '#',
  },
  {
    date: 'Dec 2025',
    category: 'Innovation',
    title: '28-Day ADU: Revolutionizing Home Construction',
    excerpt:
      'Inside the program compressing design, permitting support, and factory build into a predictable timeline for accessory dwelling units.',
    href: '#',
  },
  {
    date: 'Nov 2025',
    category: 'Industry',
    title: 'Modular Construction Trends in 2026',
    excerpt:
      'From data-rich prefab to integrated MEP: the signals procurement teams and developers are watching in the year ahead.',
    href: '#',
  },
  {
    date: 'Oct 2025',
    category: 'DoD',
    title: 'SDVOSB Certification: What It Means for Contractors',
    excerpt:
      'Why SDVOSB status matters for set-asides, teaming, and compliance—and how to evaluate partners with verified credentials.',
    href: '#',
  },
];
---

<Layout
  title="News & Insights"
  description="Industry updates, company announcements, and expert analysis from Global Modular Partners."
>
  <Header slot="header" />

  <!-- Hero -->
  <section class="hero-wrap">
    <div class="hero-inner">
      <div class="hero-content">
        <nav class="breadcrumb" aria-label="Breadcrumb">
          <a href="/">Home</a>
          <span class="breadcrumb__sep">/</span>
          <span>News & Insights</span>
        </nav>
        <h1 class="hero-title fade-up">Latest News &amp;<br /><span class="hero-title-sub">Industry Insights</span></h1>
        <p class="hero-sub fade-up">Stay informed with company updates, project milestones, and trends shaping the future of modular construction.</p>
        <div class="hero-actions fade-up">
          <a href="#articles" class="mnc-btn mnc-btn--dark">
            Latest articles
            <svg fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M9 6l6 6-6 6" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"/></svg>
          </a>
          <a href="#subscribe" class="mnc-btn mnc-btn--stroke">
            Get updates
            <svg fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M9 6l6 6-6 6" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>


  <!-- Articles -->
  <section id="articles" class="news-articles section-gap">
    <div class="mnc-container">
      <div class="news-articles__head">
        <span class="section-tag">Editorial</span>
        <h2 class="section-title">Selected perspectives <span class="section-span">for leaders in government and development</span></h2>
      </div>

      <div class="news-articles__grid">
        {articles.map((article) => (
          <article class="nc">
            <div class="nc__img">
              <div class="nc__img-shine" aria-hidden="true"></div>
              <span class="nc__date">{article.date}</span>
            </div>
            <div class="nc__body">
              <span class="nc__cat">{article.category}</span>
              <div class="nc__divider"></div>
              <h3 class="nc__title"><a href={article.href}>{article.title}</a></h3>
              <p class="nc__excerpt">{article.excerpt}</p>
              <a href={article.href} class="nc__more">Read more &rarr;</a>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- Subscribe -->
  <section id="subscribe" class="news-subscribe section-gap">
    <div class="mnc-container">
      <div class="news-subscribe__panel">
        <div class="news-subscribe__left">
          <h2 class="section-title" style="font-size:clamp(24px,3vw,36px);">Subscribe for insights</h2>
          <p class="news-subscribe__desc">
            Receive quarterly industry briefs, program announcements, and compliance-relevant
            updates. Tell us your sector—we'll tailor what you see.
          </p>
        </div>
        <div class="news-subscribe__right">
          <a href="/contact" class="al-btn-arrow">
            <span>Subscribe via contact</span>
            <span class="al-btn-arrow__icon" aria-hidden="true"><svg fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"/></svg></span>
          </a>
          <p class="news-subscribe__note">
            Prefer email? Use the contact form and mention "newsletter" in your message.
          </p>
        </div>
      </div>
    </div>
  </section>

  <PageCTA
    eyebrow="More from GMP"
    title="If you're still here,<br/>here's where else to look."
    image="/images/showcase/ri-legacy-about.jpg"
    links={[
      { label: 'Our Projects', href: '/projects' },
      { label: 'About GMP', href: '/about' },
    ]}
  />

  <Footer slot="footer" />
</Layout>

<style>
  /* ── Hero ── */
  .hero-wrap { padding: 0; }
  .hero-inner {
    height: 560px;
    border-radius: 0;
    background: linear-gradient(165deg, var(--navy-dark) 0%, var(--navy) 45%, var(--navy-light) 100%);
    display: flex;
    align-items: flex-end;
    position: relative;
    overflow: hidden;
  }
  .hero-inner::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 70% 20%, rgba(224,161,53,.08), transparent 60%),
                radial-gradient(ellipse at 20% 80%, rgba(0,0,0,.15), transparent 60%);
    pointer-events: none;
  }
  .hero-content {
    position: relative;
    z-index: 1;
    padding: 120px 1.5rem 60px max(1.5rem, calc(50vw - 620px + 1.5rem - 16px));
    width: 100%;
  }
  .breadcrumb {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    color: rgba(255,255,255,.5);
    margin-bottom: 20px;
  }
  .breadcrumb a {
    color: rgba(255,255,255,.5);
    text-decoration: none;
    transition: color 0.2s;
  }
  .breadcrumb a:hover { color: var(--amber); }
  .breadcrumb__sep { color: rgba(255,255,255,.25); }
  .hero-wrap .section-tag { color: rgba(255,255,255,.6); }
  .hero-wrap .section-tag::before { background: var(--amber); }
  .hero-title {
    font-family: var(--font-secondary);
    font-size: clamp(36px, 5vw, 56px);
    font-weight: 800;
    line-height: 1.15;
    color: var(--white);
    max-width: 700px;
    margin-bottom: 16px;
  }

  .hero-title-sub {
    color: rgba(255, 255, 255, 0.5);
    font-weight: 400;
  }
  .hero-sub {
    color: rgba(255,255,255,.65);
    font-size: 18px;
    line-height: 1.7;
    max-width: 560px;
    margin-bottom: 32px;
  }
  .hero-actions { display: flex; gap: 12px; flex-wrap: wrap; }

  /* ── MNC Buttons ── */
  .mnc-btn {
    display: inline-flex;
    align-items: center;
    padding: 14px 28px;
    font-family: var(--font-primary);
    font-size: 15px;
    font-weight: 600;
    border-radius: 4px;
    text-decoration: none;
    transition: all 200ms ease;
    cursor: pointer;
    border: 1px solid transparent;
    line-height: 1;
  }
  .mnc-btn__wrap {
    display: inline-flex;
    align-items: center;
    gap: 10px;
  }
  .mnc-btn__icon {
    display: inline-flex;
    width: 18px;
    height: 18px;
    transition: transform 200ms ease;
  }
  .mnc-btn__icon svg {
    width: 100%;
    height: 100%;
  }
  .mnc-btn:hover .mnc-btn__icon {
    transform: translateX(3px);
  }
  .mnc-btn--dark {
    background: var(--amber);
    color: var(--navy);
    border-color: var(--amber);
    font-weight: 700;
  }
  .mnc-btn--dark:hover {
    background: var(--amber-dark);
    border-color: var(--amber-dark);
    color: var(--white);
    box-shadow: 0 4px 16px rgba(224,161,53,.35);
  }
  .mnc-btn--outline {
    background: transparent;
    border-color: var(--navy);
    color: var(--navy);
  }
  .mnc-btn--outline:hover {
    background: var(--navy);
    color: #fff;
  }
  .mnc-btn--stroke {
    background: transparent;
    border-color: rgba(255,255,255,.3);
    color: #fff;
  }
  .mnc-btn--stroke:hover {
    border-color: var(--amber);
    color: var(--amber);
  }

  /* ── Container & Section ── */
  .mnc-container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }
  .section-gap {
    padding: 100px 0;
  }
  /* .section-tag / .section-title / .section-span now inherit from global.css (aeline) */

  /* ── Articles Section (aeline) ── */
  .news-articles {
    background: var(--paper);
  }
  .news-articles__head {
    text-align: center;
    margin-bottom: 56px;
    max-width: 52rem;
    margin-left: auto;
    margin-right: auto;
  }
  .news-articles__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  @media (max-width: 1024px) {
    .news-articles__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 640px) {
    .news-articles__grid {
      grid-template-columns: 1fr;
    }
    .section-gap {
      padding: 64px 0;
    }
  }

  /* ── Article Card (aeline) ── */
  .nc {
    display: flex;
    flex-direction: column;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: var(--radius-xl);
    overflow: hidden;
    transition: transform 300ms ease, box-shadow 300ms ease, border-color 300ms ease;
  }
  .nc:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
    border-color: #d6d6d1;
  }
  .nc__img {
    position: relative;
    height: 200px;
    background: var(--ink);
    margin: 12px 12px 0;
    border-radius: var(--radius-lg);
    overflow: hidden;
    display: flex;
    align-items: flex-end;
    padding: 16px;
  }
  .nc__img-shine {
    position: absolute;
    inset: -40%;
    background: radial-gradient(ellipse at 30% 25%, rgba(240,198,69,.18) 0%, transparent 55%);
  }
  .nc__date {
    position: relative;
    z-index: 1;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,.7);
  }
  .nc__body {
    position: relative;
    background: transparent;
    border: none;
    padding: 24px;
    z-index: 2;
    display: flex;
    flex-direction: column;
    flex: 1;
    gap: 14px;
  }
  .nc__cat {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    width: fit-content;
    padding: 0;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: var(--tracking-label);
    color: var(--ink);
    background: transparent;
    border-radius: 0;
  }
  .nc__cat::before {
    content: '';
    width: 6px;
    height: 6px;
    background: var(--ink);
    display: inline-block;
  }
  .nc__divider {
    border-top: 1px solid var(--line);
  }
  .nc__title {
    font-family: var(--font-display);
    font-size: 1.2rem;
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.3;
    color: var(--ink);
    margin: 0;
  }
  .nc__title a {
    color: inherit;
    text-decoration: none;
    transition: opacity 200ms ease;
  }
  .nc__title a:hover { opacity: .75; }
  .nc__excerpt {
    flex: 1;
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--muted);
    margin: 0;
  }
  .nc__more {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--ink);
    padding-bottom: 2px;
    border-bottom: 1px solid var(--ink);
    width: fit-content;
    transition: gap 200ms ease;
  }
  .nc__more:hover { gap: 10px; }

  /* ── Subscribe Section (aeline dark CTA card) ── */
  .news-subscribe {
    background: var(--paper);
  }
  .news-subscribe__panel {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 48px;
    align-items: center;
    background: var(--ink);
    color: var(--on-ink);
    border-radius: var(--radius-2xl);
    border: none;
    padding: clamp(2rem, 5vw, 4rem);
    max-width: 1080px;
    margin: 0 auto;
  }
  .news-subscribe__panel .section-title {
    color: var(--on-ink);
  }
  .news-subscribe__panel .section-span {
    color: var(--on-ink-muted);
  }
  @media (max-width: 768px) {
    .hero-inner { height: 480px; }
    .hero-content { padding: 80px 1.5rem 40px; }
    .news-subscribe__panel {
      grid-template-columns: 1fr;
      padding: 32px 24px;
      gap: 32px;
    }
  }
  .news-subscribe__desc {
    font-size: 1rem;
    line-height: 1.65;
    color: var(--on-ink-muted);
    margin: 16px 0 0;
    max-width: 38ch;
  }
  .news-subscribe__right {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 16px;
  }
  .news-subscribe__right .al-btn-arrow {
    background: var(--paper);
    color: var(--ink);
    border-color: var(--paper);
  }
  .news-subscribe__right .al-btn-arrow:hover {
    background: transparent;
    color: var(--on-ink);
    border-color: var(--on-ink);
  }
  .news-subscribe__note {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    line-height: 1.55;
    letter-spacing: var(--tracking-label);
    text-transform: uppercase;
    color: var(--on-ink-muted);
    margin: 0;
    max-width: 280px;
  }
  @media (max-width: 768px) {
    .news-subscribe__right {
      align-items: stretch;
    }
    .news-subscribe__right .mnc-btn {
      justify-content: center;
    }
    .news-subscribe__note {
      max-width: none;
    }
  }
</style>
